# Project

Boilerplate for AI-augmented projects — Python backend, TypeScript frontend, Claude Code workflow.

## Rules

- Always write in English: code, comments, docs, commit messages.
- After any change, check if README.md needs updating and update it if so.
- Follow the development process in `docs/development-process.md`: branch naming, commit convention, review workflow.

## Stack

- Backend: Python (`backend/`)
- Frontend: TypeScript (`frontend/`)

## Commands

<!-- Fill in when branching this template — list the commands you run most. -->

- Backend:  e.g. `cd backend && python -m uvicorn main:app --reload`
- Frontend: e.g. `cd frontend && npm run dev`
- Tests:    e.g. `cd backend && pytest` / `cd frontend && npm test`
