---
description: One-line description of when Claude should invoke this skill
argument-hint: <optional-argument>
# disable-model-invocation: true  # uncomment to allow only manual /example invocation
---

<!-- Skill body. Use $ARGUMENTS for what was typed after /example. -->
<!-- Use !`shell command` to inject shell output into the prompt. -->

Perform the task described above with the provided argument: $ARGUMENTS
